---
phase: 03-data-integrity-business-logic
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/admin.routes.ts
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "Admin stats no longer silently truncate at Supabase's default 1000-row cap."
    - "Admin users no longer silently truncate joined datasets at the default row cap."
    - "Both admin endpoints keep their current non-paginated response shapes for the existing frontend consumers."
  artifacts:
    - path: "server/routes/admin.routes.ts"
      provides: "Explicit high-limit admin reads for stats and users endpoints"
      contains: ["/api/admin/stats", "/api/admin/users", "limit(5000)"]
  key_links:
    - from: "server/routes/admin.routes.ts"
      to: "client/src/components/admin/dashboard-tab.tsx"
      via: "stats endpoint keeps existing JSON shape while adding explicit query caps"
      pattern: "res\.json\(\{"
    - from: "server/routes/admin.routes.ts"
      to: "client/src/components/admin/users-tab.tsx"
      via: "users endpoint still returns `{ users: ... }` while every large table read uses the shared limit"
      pattern: "res\.json\(\{\s*users"
---

<objective>
Make the two admin reporting endpoints survive datasets above 1000 rows without changing the payload shapes the current frontend already consumes.

Purpose: Close DATA-03 with the surgical cap-based approach chosen in research, avoiding premature pagination or SQL aggregation while eliminating silent truncation at scale.
Output: Explicit high `.limit(...)` guards in `server/routes/admin.routes.ts` for both `/api/admin/stats` and `/api/admin/users`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/debug/full-system-bug-audit.md
@.planning/phases/03-data-integrity-business-logic/03-RESEARCH.md
@.planning/phases/03-data-integrity-business-logic/03-VALIDATION.md
@server/routes/admin.routes.ts
@client/src/components/admin/dashboard-tab.tsx
@client/src/components/admin/users-tab.tsx

<interfaces>
From `client/src/components/admin/dashboard-tab.tsx`:
```typescript
useQuery<AdminStats>({
  queryKey: ["/api/admin/stats", rangeMode, windowDays, ...],
  queryFn: () => adminFetch(`/api/admin/stats?${statsQuerySuffix}`),
});
```

From `client/src/components/admin/users-tab.tsx`:
```typescript
useQuery<{ users: AdminUser[] }>({
  queryKey: ["/api/admin/users"],
  queryFn: () => adminFetch("/api/admin/users"),
});
```

From `server/routes/admin.routes.ts`:
```typescript
const [usersRes, postsRes, brandsRes, usageRes, creditsRes, tokenPricing] = await Promise.all([...]);

const [
  { data: profiles },
  { data: brands },
  { data: posts },
  { data: credits },
  { data: usageEvents },
  { data: affiliateSettings },
  { data: billingProfiles },
] = await Promise.all([...]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one shared high-limit guard to the admin stats reads</name>
  <files>server/routes/admin.routes.ts</files>
  <action>Implement DATA-03 per LD-05 and LD-06 by introducing one shared ceiling constant in `server/routes/admin.routes.ts` and applying it to every large-table select used by `GET /api/admin/stats` (`profiles`, `posts`, `brands`, `usage_events`, and `user_credits`). Keep the current `count: "exact"` usage and in-memory calculations intact, and do not replace this endpoint with pagination, SQL aggregation, or a repository abstraction in this phase.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Against a dataset above 1000 rows, call `GET /api/admin/stats` and confirm totals such as `totalUsers`, `totalPosts`, and analytics counts still reflect the larger dataset while the response shape consumed by `client/src/components/admin/dashboard-tab.tsx` remains unchanged.</manual>
  </verify>
  <done>The stats endpoint now uses an explicit shared limit high enough to avoid Supabase's silent 1000-row truncation without changing the current JSON contract.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same limit guard to the admin users dataset fan-out</name>
  <files>server/routes/admin.routes.ts</files>
  <action>Finish DATA-03 by applying the same shared limit to each large read inside `GET /api/admin/users` (`profiles`, `brands`, `posts`, `user_credits`, `usage_events`, `affiliate_settings`, and `user_billing_profiles`). Preserve the existing joined in-memory assembly and final `{ users: [...] }` payload so `client/src/components/admin/users-tab.tsx` continues to work without pagination changes.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Against the same dataset, call `GET /api/admin/users` and confirm the endpoint still returns the current `{ users: [...] }` shape while joined counts and user-level aggregates are no longer silently clipped at 1000 rows.</manual>
  </verify>
  <done>Both admin endpoints share the same explicit high limit, removing the immediate truncation bug while keeping the current frontend contracts intact.</done>
</task>

</tasks>

<verification>
Run `npm run check` after each task. Then manually verify `GET /api/admin/stats` and `GET /api/admin/users` against a dataset above 1000 rows, confirming the existing dashboard and users payload shapes remain unchanged while totals no longer silently reflect only the first 1000 rows.
</verification>

<success_criteria>
- DATA-03 is satisfied with one documented shared admin read limit of `5000` across the affected stats and users queries.
- `client/src/components/admin/dashboard-tab.tsx` and `client/src/components/admin/users-tab.tsx` still consume the same response shapes.
- True pagination/aggregation remains deferred to `PERF-01`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-integrity-business-logic/03-data-integrity-business-logic-03-SUMMARY.md`
</output>

---
phase: 02-supabase-client-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/edit.routes.ts
  - server/routes/admin.routes.ts
autonomous: true
requirements:
  - SBC-03
  - DATA-04
must_haves:
  truths:
    - "Editing an image post uploads the new image successfully instead of failing on storage permissions."
    - "The edit flow keeps user-owned row inserts scoped to the authenticated user while using admin storage for file writes."
    - "The admin color migration endpoint reports RPC failures honestly instead of returning success on error."
  artifacts:
    - path: "server/routes/edit.routes.ts"
      provides: "Image-edit storage upload path aligned with existing admin-storage behavior"
      contains: ["createAdminSupabase", "storage", "getPublicUrl", "post_versions"]
    - path: "server/routes/admin.routes.ts"
      provides: "Explicit RPC error branch for migrate-colors"
      contains: ["rpc(\"exec\"", "error1", "res.status"]
  key_links:
    - from: "server/routes/edit.routes.ts"
      to: "user_assets"
      via: "image branch uses admin storage client while post_versions insert remains on user client"
      pattern: "createAdminSupabase|storage\\.from\\(\"user_assets\"\\)"
    - from: "server/routes/edit.routes.ts"
      to: "post_versions"
      via: "owned-row insert still uses supabase.from(\"post_versions\")"
      pattern: "supabase[\\s\\S]*from\\(\"post_versions\"\\)[\\s\\S]*insert"
    - from: "server/routes/admin.routes.ts"
      to: "sb.rpc(\"exec\")"
      via: "immediate error1 branch logs and returns failure"
      pattern: "if \\(error1\\)"
---

<objective>
Align the edit image branch with the repo's admin-storage pattern and make the admin color migration endpoint fail honestly when Supabase RPC returns an error.

Purpose: Remove the remaining wrong-client upload path and the silent-success RPC branch without widening route privileges or refactoring unrelated auth logic.
Output: A surgical edit-route storage fix and explicit migrate-colors failure handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/debug/full-system-bug-audit.md
@.planning/phases/02-supabase-client-correctness/02-RESEARCH.md
@.planning/phases/02-supabase-client-correctness/02-VALIDATION.md
@server/routes/edit.routes.ts
@server/routes/generate.routes.ts
@server/routes/admin.routes.ts
@server/storage.ts
@server/supabase.ts

<interfaces>
From `server/supabase.ts`:
```typescript
export function createAdminSupabase(): SupabaseClient;
```

From `server/routes/edit.routes.ts`:
```typescript
const adminSb = createAdminSupabase();
publicUrl = await uploadFile(
    adminSb,
    "user_assets",
    `${user.id}/${versionId}.mp4`,
    videoResult.buffer,
    "video/mp4"
);

const { data: newVersion, error: versionError } = await supabase
    .from("post_versions")
    .insert({
        post_id: post_id,
        version_number: nextVersionNumber,
        image_url: publicUrl,
        thumbnail_url: thumbnailUrl,
        edit_prompt: edit_prompt,
    })
    .select()
    .single();
```

From `server/routes/admin.routes.ts`:
```typescript
const { error: error1 } = await sb.rpc("exec", {
    sql: "ALTER TABLE public.brands ADD COLUMN IF NOT EXISTS color_4 text;",
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move only the edit image storage calls to an admin client</name>
  <files>server/routes/edit.routes.ts</files>
  <action>Implement SBC-03 per LD-02 and LD-03 by aligning the image-edit branch with the existing admin-storage patterns already used in the video branch and generate route. Introduce or reuse an admin storage client inside the image branch, then swap only the four `supabase.storage` call sites involved in uploading the optimized image, reading its public URL, uploading the thumbnail, and reading the thumbnail public URL. Keep owned-row reads and the `post_versions` insert on the existing user-scoped `supabase` client, and do not refactor auth extraction or the rest of the route.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Run an image edit on an owned post through the same API path used by the app and verify: (1) the edited image upload succeeds, (2) the thumbnail upload succeeds or logs only the existing non-critical warning path, (3) the returned URLs resolve from `user_assets`, and (4) a new `post_versions` row is inserted for the post.</manual>
  </verify>
  <done>The image-edit storage path now uses an admin Supabase client like the sibling video/generate flows, while the route still uses the user-scoped client for owned-row database operations.</done>
</task>

<task type="auto">
  <name>Task 2: Branch on migrate-colors RPC failure before returning success</name>
  <files>server/routes/admin.routes.ts</files>
  <action>Implement DATA-04 per LD-04 by adding an immediate `if (error1)` branch after `sb.rpc("exec", ...)` in `POST /api/admin/migrate-colors`. Log the RPC error, return a failure response (prefer HTTP 500 with the existing manual-SQL note), and preserve the current follow-up column check and success payload only for the no-error path. Do not replace the endpoint with a different migration mechanism in this phase.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Call `POST /api/admin/migrate-colors` as an admin in an environment where the `exec` RPC is unavailable or forced to fail. Expect the response to be a non-success failure payload with the manual SQL note, and confirm the server logs the RPC error instead of returning `success: true`.</manual>
  </verify>
  <done>`migrate-colors` no longer reports success when `sb.rpc("exec", ...)` returns an error, and the failure path logs the problem and returns an honest response.</done>
</task>

</tasks>

<verification>
Run `npm run check` after each task. For manual verification, exercise an image edit through the live edit route to confirm admin-storage uploads work without breaking the user-scoped version insert, then trigger a failing `POST /api/admin/migrate-colors` call and confirm the endpoint logs and returns failure instead of success.
</verification>

<success_criteria>
- SBC-03 is satisfied with admin storage uploads in the image-edit branch only.
- DATA-04 is satisfied with explicit RPC error handling in `migrate-colors`.
- No broad route/auth refactor is introduced beyond the locked surgical fixes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-client-correctness/02-supabase-client-correctness-02-SUMMARY.md`
</output>

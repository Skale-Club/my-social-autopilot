---
phase: 01-security-auth-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/config.routes.ts
  - server/routes/settings.routes.ts
  - server/routes/stripe.routes.ts
  - server/quota.ts
autonomous: true
requirements:
  - SEC-03
  - QUOT-02
  - QUOT-03
must_haves:
  truths:
    - "Stripe webhook requests with a non-Buffer raw body fail fast with 400 before signature verification."
    - "Users blocked by inactive subscriptions in subscription overage mode receive the `inactive_subscription` denial reason."
    - "GET /api/settings resolves through a single canonical handler that still includes the landing icon URL in the response."
  artifacts:
    - path: "server/routes/stripe.routes.ts"
      provides: "Preflight rawBody guard before Stripe signature verification"
      contains: ["Buffer.isBuffer", "constructEvent"]
    - path: "server/quota.ts"
      provides: "subscription_overage denial reason selection"
      contains: ["inactive_subscription"]
    - path: "server/routes/config.routes.ts"
      provides: "public config route without duplicate /api/settings handler"
    - path: "server/routes/settings.routes.ts"
      provides: "canonical public settings response including landing icon merge"
  key_links:
    - from: "server/routes/stripe.routes.ts"
      to: "stripe.webhooks.constructEvent"
      via: "Buffer.isBuffer preflight guard"
      pattern: "Buffer\\.isBuffer.*rawBody"
    - from: "server/quota.ts"
      to: "CreditStatus.denial_reason"
      via: "subscription_overage inactive subscription branch"
      pattern: "inactive_subscription"
    - from: "server/routes/index.ts"
      to: "server/routes/settings.routes.ts"
      via: "config.routes no longer shadows GET /api/settings"
      pattern: "router\\.use\\(configRoutes\\)|router\\.use\\(settingsRoutes\\)"
---

<objective>
Apply the remaining targeted hardening fixes to webhook validation, quota denial messaging, and the duplicated public settings route.

Purpose: Close the route and billing edge cases that currently return the wrong response path or the wrong denial reason.
Output: One canonical `GET /api/settings` handler, a guarded Stripe webhook endpoint, and corrected inactive subscription messaging.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-hardening/01-CONTEXT.md
@.planning/phases/01-security-auth-hardening/01-RESEARCH.md
@.planning/phases/01-security-auth-hardening/01-VALIDATION.md
@server/routes/index.ts
@server/routes/config.routes.ts
@server/routes/settings.routes.ts
@server/routes/stripe.routes.ts
@server/quota.ts

<interfaces>
From `server/routes/index.ts`:
```typescript
export function createApiRouter(): Router;
```

From `server/quota.ts`:
```typescript
export interface CreditStatus {
  denial_reason?: "inactive_subscription" | "usage_budget_reached" | "upgrade_required" | null;
}

export async function checkCredits(
  userId: string,
  operationType: "generate" | "edit" | "transcribe",
  isVideo?: boolean,
): Promise<CreditStatus>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stripe webhook raw body preflight guard</name>
  <files>server/routes/stripe.routes.ts</files>
  <action>Implement SEC-03 per D-05 by adding a `Buffer.isBuffer((req as any).rawBody)` guard before `stripe.webhooks.constructEvent()`. If the body is not a Buffer, return HTTP 400 with a clear JSON error and stop before Stripe SDK verification runs. Keep the existing webhook secret validation and downstream event handling flow unchanged.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Run the server and send `curl -i -X POST http://localhost:8888/api/stripe/webhook -H "Content-Type: application/json" -H "Stripe-Signature: test" -d '{"id":"evt_test"}'`. Expect HTTP 400 with the new raw-body validation message, and do not accept the old `Webhook Error: ...` signature-mismatch response as a pass.</manual>
  </verify>
  <done>`stripe.routes.ts` rejects non-Buffer `rawBody` values with 400 before calling `constructEvent()`.</done>
</task>

<task type="auto">
  <name>Task 2: Correct inactive subscription denial reason</name>
  <files>server/quota.ts</files>
  <action>Implement QUOT-02 per D-09 by changing only the inactive subscription branch in `checkCredits()` for `subscription_overage` billing mode so `denialReason` becomes `"inactive_subscription"`. Do not alter the rest of the quota branching, budget checks, or allowed/denied logic.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Using a seeded user whose billing profile is `subscription_overage`, subscription status is inactive/canceled, and included credits are exhausted, run a one-off `node --import tsx` script that calls `checkCredits(userId, "generate")`. Expect the returned object to include `allowed: false` and `denial_reason: "inactive_subscription"`; repeat with an active subscription whose usage budget is exceeded and expect `denial_reason: "usage_budget_reached"`.</manual>
  </verify>
  <done>The inactive subscription path reports `inactive_subscription` and existing budget-blocked behavior stays unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate GET /api/settings to the canonical settings router</name>
  <files>server/routes/config.routes.ts, server/routes/settings.routes.ts</files>
  <action>Implement QUOT-03 per D-06, D-07, and D-08 by removing the duplicate `GET /api/settings` handler from `config.routes.ts` while preserving `GET /api/config`. Leave `settings.routes.ts` as the canonical public settings handler with its inline `landing_content.icon_url` merge. Do not introduce a service extraction or alter route registration order in `server/routes/index.ts` unless required for compilation.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Run the app and call `curl http://localhost:8888/api/settings`. Confirm the response is served by `settings.routes.ts`, returns exactly one payload, and includes `favicon_url` populated from `landing_content.icon_url` when present. Then call `curl http://localhost:8888/api/config` and confirm it still returns `supabaseUrl` and `supabaseAnonKey` after the duplicate `/api/settings` handler is removed from `config.routes.ts`.</manual>
  </verify>
  <done>Only one `GET /api/settings` handler remains in the codebase and it is the richer handler in `settings.routes.ts`.</done>
</task>

</tasks>

<verification>
Run `npm run check` after each task. For smoke verification, confirm `/api/stripe/webhook` returns the raw-body 400 before Stripe signature verification, `checkCredits()` reports `inactive_subscription` for the seeded inactive-subscription scenario, and `GET /api/settings` returns the canonical payload with `favicon_url` merged from `landing_content.icon_url` while `/api/config` still works.
</verification>

<success_criteria>
- SEC-03 is satisfied with a preflight rawBody Buffer guard.
- QUOT-02 is satisfied with the corrected inactive subscription denial reason.
- QUOT-03 is satisfied with one canonical `GET /api/settings` handler and no duplicate route conflict.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-hardening/01-security-auth-hardening-02-SUMMARY.md`
</output>

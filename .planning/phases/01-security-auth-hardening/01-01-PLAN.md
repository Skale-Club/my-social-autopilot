---
phase: 01-security-auth-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/middleware/auth.middleware.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Malformed Authorization headers are rejected with 401 instead of being passed through as tokens."
    - "Admin-only middleware attaches a full profile object so downstream handlers can read req.profile safely."
  artifacts:
    - path: "server/middleware/auth.middleware.ts"
      provides: "Bearer parsing, admin auth checks, and req.profile attachment"
      contains: ["extractToken", "requireAdmin", "requireAdminGuard"]
  key_links:
    - from: "server/middleware/auth.middleware.ts"
      to: "Authorization header"
      via: "startsWith('Bearer ') + slice(7)"
      pattern: "startsWith\\(\\\"Bearer \\"\\)|slice\\(7\\)"
    - from: "server/middleware/auth.middleware.ts"
      to: "req.profile"
      via: "requireAdmin attaches fetched profile after admin check"
      pattern: "\\(req as any\\)\\.profile"
---

<objective>
Harden the shared auth middleware so malformed Bearer headers are rejected consistently and admin routes receive the full authenticated profile object.

Purpose: Close the two auth primitives that currently allow malformed header handling and missing `req.profile` crashes.
Output: Updated auth middleware with corrected token parsing and populated admin request context.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-hardening/01-CONTEXT.md
@.planning/phases/01-security-auth-hardening/01-RESEARCH.md
@.planning/phases/01-security-auth-hardening/01-VALIDATION.md
@server/middleware/auth.middleware.ts
@server/middleware/admin.middleware.ts
@shared/schema.ts

<interfaces>
From `server/middleware/auth.middleware.ts`:
```typescript
export interface AuthenticatedRequest extends Request {
    user: User;
    supabase: SupabaseClient;
    profile: Profile;
}

export function extractToken(req: AuthenticatedRequest): string | null;
export async function requireAdmin(req: AuthenticatedRequest, res: Response, next: NextFunction): Promise<void>;
export async function requireAdminGuard(req: Request, res: Response): Promise<{ userId: string } | null>;
```

From `shared/schema.ts`:
```typescript
export type Profile = z.infer<typeof profileSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Bearer extraction at every auth middleware call site</name>
  <files>server/middleware/auth.middleware.ts</files>
  <action>Implement SEC-01 per D-01 and D-02 by changing `extractToken()` to accept `Request`, reject any authorization header that does not start with `"Bearer "`, and return `slice(7)` when valid. Update the inline extraction inside `requireAdminGuard()` to use the same `startsWith("Bearer ")` + `slice(7)` pattern instead of `.replace("Bearer ", "")`. Keep the fix surgical inside this file only; do not refactor middleware structure or introduce helper abstractions beyond the existing function.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Start the dev server and verify both Bearer parsing paths: (1) `curl -i -H "Authorization: BearerTOKEN" http://localhost:8888/api/generate` should return 401, confirming `extractToken()` rejects malformed headers; (2) `curl -i -H "Authorization: BearerTOKEN" http://localhost:8888/api/admin/stats` should also return 401, confirming the inline `requireAdminGuard()` path no longer accepts malformed headers.</manual>
  </verify>
  <done>`extractToken()` accepts `Request`, malformed Bearer headers resolve to null, and no `.replace("Bearer ", "")` call site remains in `auth.middleware.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Attach full admin profile inside requireAdmin</name>
  <files>server/middleware/auth.middleware.ts</files>
  <action>Implement SEC-02 per D-03 and D-04 by keeping `requireAdmin()` as a standalone middleware, fetching the admin profile with `createAdminSupabase()` and `select("*")` + `.maybeSingle()`, validating `profile?.is_admin`, and attaching `req.profile` after the admin check passes. Preserve the existing `req.user` and user-scoped `req.supabase` attachments; do not refactor `requireAdmin()` to call `authenticateUser()` internally.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Use a one-off local script with `node --import tsx` to import `requireAdmin()` and invoke it with a mocked Express `req/res/next`, a valid admin `Authorization: Bearer <token>` header, and spies for `next()`/`res.status()`. Expect `next()` to run once, `res.status()` not to be called, and `req.profile?.is_admin === true`; then repeat with a non-admin token and expect HTTP 403.</manual>
  </verify>
  <done>`requireAdmin()` uses the admin client for the profile fetch, attaches the full profile object to `req.profile`, and downstream admin handlers can safely read profile fields.</done>
</task>

</tasks>

<verification>
Run `npm run check` after each task. For smoke verification, confirm malformed auth headers return 401 on both `/api/generate` and `/api/admin/stats`, and verify `requireAdmin()` populates `req.profile` in a one-off `node --import tsx` middleware invocation with an admin token.
</verification>

<success_criteria>
- SEC-01 is satisfied across both `extractToken()` and `requireAdminGuard()`.
- SEC-02 is satisfied with `req.profile` populated after successful admin auth.
- No auth middleware refactor beyond the locked surgical changes is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-hardening/01-security-auth-hardening-01-SUMMARY.md`
</output>

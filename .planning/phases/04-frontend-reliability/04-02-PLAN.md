---
phase: 04-frontend-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/auth.tsx
autonomous: true
requirements:
  - FE-02
  - FE-05
  - FE-06
must_haves:
  truths:
    - "Telegram signup notification fires only when a profile is created for the first time, not on login or token refresh."
    - "`fetchUserData()` always clears the loading state, even when profile or brand reads fail mid-flow."
    - "`refreshProfile()` tolerates a temporarily missing profile row instead of throwing a 406-style single-row error."
  artifacts:
    - path: "client/src/lib/auth.tsx"
      provides: "Stable auth bootstrap lifecycle for signup side effects, loading teardown, and tolerant profile refresh"
      contains: ["fetchUserData", "notifyTelegramOnSignup", "setLoading(false)", "refreshProfile"]
  key_links:
    - from: "client/src/lib/auth.tsx"
      to: "/api/telegram/notify-signup"
      via: "notify call occurs only in the just-created profile branch"
      pattern: "notifyTelegramOnSignup|insert\(insertProfilePayload\)"
    - from: "client/src/lib/auth.tsx"
      to: "client/src/App.tsx"
      via: "AuthProvider loading state reliably transitions to `false` after bootstrap attempts"
      pattern: "setLoading\(false\)|finally"
---

<objective>
Stabilize the auth bootstrap helper so signup side effects, loading teardown, and profile refresh all match the intended client lifecycle.

Purpose: Close FE-02, FE-05, and FE-06 with narrow edits in `client/src/lib/auth.tsx`, preserving the existing auth architecture while removing repeated signup notifications, endless spinners, and brittle single-row reads.
Output: A surgical auth-context update that only changes the broken lifecycle branches.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/debug/full-system-bug-audit.md
@.planning/phases/04-frontend-reliability/04-RESEARCH.md
@.planning/phases/04-frontend-reliability/04-VALIDATION.md
@client/src/lib/auth.tsx
@client/src/lib/supabase.ts

<interfaces>
From `client/src/lib/auth.tsx`:
```typescript
const fetchUserData = useCallback(async (userId: string, session: Session | null = null) => {
  const [profileRes, brandRes] = await Promise.all([
    sb.from("profiles").select("*").eq("id", userId).maybeSingle(),
    sb.from("brands").select("*").eq("user_id", userId).maybeSingle(),
  ]);
}, [notifyTelegramOnSignup, tryClaimAffiliateReferral]);

const refreshProfile = useCallback(async () => {
  if (!user) return;
  const { data } = await sb.from("profiles").select("*").eq("id", user.id).single();
  setProfile(data);
}, [user]);
```

From `client/src/lib/auth.tsx` existing tolerant read pattern:
```typescript
sb.from("profiles").select("*").eq("id", userId).maybeSingle()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fire Telegram signup notification only from the profile-creation branch</name>
  <files>client/src/lib/auth.tsx</files>
  <action>Implement FE-02 per LD-03 by moving `notifyTelegramOnSignup(...)` into the branch that successfully creates a missing profile inside `fetchUserData()`. Keep the existing `notifyTelegramOnSignup` helper and auth flow structure, but ensure logins, token refreshes, and app reloads for existing users never call it. Do not add localStorage/sessionStorage dedupe or redesign notification delivery.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Create a brand-new account and inspect the browser network tab for `/api/telegram/notify-signup`. Confirm exactly one request on the first profile creation, then refresh and sign in again to confirm no duplicate notification fires.</manual>
  </verify>
  <done>The Telegram signup endpoint is called only when a new profile row is created, eliminating repeated notifications on existing-session auth events.</done>
</task>

<task type="auto">
  <name>Task 2: Move auth loading teardown into a `finally` block</name>
  <files>client/src/lib/auth.tsx</files>
  <action>Implement FE-05 per LD-06 by moving `setLoading(false)` into a `finally` block inside `fetchUserData()` and keeping the surrounding profile/brand state flow intact. Preserve the current `try/catch` logging and state updates; do not rewrite the hook into reducers, add retries, or change the external `AuthState` contract.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Force a `fetchUserData()` failure path during auth bootstrap and confirm the loading screen clears instead of spinning forever, even when the profile or brand fetch errors.</manual>
  </verify>
  <done>`fetchUserData()` always transitions `loading` to `false` after completion or failure, so auth bootstrap cannot strand the app in an endless spinner.</done>
</task>

<task type="auto">
  <name>Task 3: Make `refreshProfile()` tolerant of missing rows</name>
  <files>client/src/lib/auth.tsx</files>
  <action>Implement FE-06 per LD-07 by changing only `refreshProfile()` from `.single()` to `.maybeSingle()` so it matches the rest of the profile-read behavior. Keep `refreshBrand()` and the rest of the auth provider unchanged; do not widen this into a broader profile creation retry system.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Trigger `refreshProfile()` in a flow where the profile row is absent or delayed and confirm the app no longer throws a 406-style single-row error path.</manual>
  </verify>
  <done>`refreshProfile()` can safely receive a missing profile row and update state without throwing the brittle `.single()` error path.</done>
</task>

</tasks>

<verification>
Run `npm run check` after each task. Then manually verify one brand-new signup, one existing-user login/refresh cycle, one forced `fetchUserData()` error path, and one `refreshProfile()` call against a missing-row scenario to confirm the auth context now behaves deterministically.
</verification>

<success_criteria>
- FE-02 is satisfied by tying Telegram notification to successful profile creation only.
- FE-05 is satisfied by a `finally`-based loading teardown in `fetchUserData()`.
- FE-06 is satisfied by changing only `refreshProfile()` to `.maybeSingle()`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-reliability/04-frontend-reliability-02-SUMMARY.md`
</output>

---
phase: 04-frontend-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/App.tsx
autonomous: true
requirements:
  - FE-01
  - FE-07
must_haves:
  truths:
    - "An authenticated admin who directly opens `/admin/*` stays in the admin shell instead of falling back to `/dashboard`."
    - "The admin sidebar/header state stays aligned with the current `/admin/*` route because `isAdminMode` is synchronized instead of bypassed."
    - "An authenticated user with `brand` loaded but `profile === null` sees a safe blocking fallback instead of crashing on `profile.is_admin`."
  artifacts:
    - path: "client/src/App.tsx"
      provides: "Route-aware private shell with explicit null-profile guard and admin-mode synchronization"
      contains: ["AppContent", "useLocation", "useAdminMode", "profile?.is_admin"]
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/lib/admin-mode.tsx"
      via: "route-synced `setAdminMode(...)` when an authenticated admin is on `/admin/*`"
      pattern: "setAdminMode\(|location\.startsWith\(\"/admin\"\)"
    - from: "client/src/App.tsx"
      to: "client/src/pages/admin.tsx"
      via: "admin shell selection before the user-shell redirect branch"
      pattern: "AdminPage|Redirect to=\"/dashboard\""
---

<objective>
Repair the private app shell so admin routes and null-profile bootstrap states behave safely without redesigning routing or admin mode.

Purpose: Close FE-01 and FE-07 with one surgical `client/src/App.tsx` change that respects the existing admin-mode feature and prevents invalid authenticated shell states.
Output: A route-synchronized `AppContent` branch that preserves the admin shell on `/admin/*` and blocks unsafe rendering when `profile` is still null.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/debug/full-system-bug-audit.md
@.planning/phases/04-frontend-reliability/04-RESEARCH.md
@.planning/phases/04-frontend-reliability/04-VALIDATION.md
@client/src/App.tsx
@client/src/lib/admin-mode.tsx
@client/src/pages/admin.tsx

<interfaces>
From `client/src/lib/admin-mode.tsx`:
```typescript
interface AdminModeState {
  isAdminMode: boolean;
  setAdminMode: (value: boolean) => void;
  toggleMode: () => void;
}
```

From `client/src/App.tsx`:
```typescript
const { user, profile, brand, loading } = useAuth();
const { isAdminMode, setAdminMode } = useAdminMode();
const [location, setLocation] = useLocation();

if (isAdminMode && profile?.is_admin && location.startsWith("/admin")) {
  const adminTabSegment = location.split("/")[2] || "dashboard";
}
```

From `client/src/App.tsx` user shell:
```typescript
<Route path="/admin">
  <Redirect to="/dashboard" />
</Route>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route-sync admin mode for direct `/admin/*` navigation</name>
  <files>client/src/App.tsx</files>
  <action>Implement FE-01 per LD-01 and LD-02 by keeping the existing `isAdminMode` feature flag but synchronizing it inside `AppContent` whenever an authenticated admin is currently on `/admin/*`. The fix must preserve the existing exit-admin button behavior and sidebar mode logic; do not remove admin mode, rewrite `client/src/lib/admin-mode.tsx`, or bypass the flag entirely. Ensure the direct-route path wins before the user-shell `/admin -> /dashboard` redirect can fire.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Sign in as an admin and hard-load `/admin/dashboard` and `/admin/users`. Confirm the admin shell renders with admin navigation instead of redirecting to `/dashboard`. Click `Exit Admin`, then revisit those URLs directly and confirm the route re-enables admin mode automatically.</manual>
  </verify>
  <done>Authenticated admins can directly enter any existing `/admin/*` route and remain in the admin shell while the existing admin-mode toggle and exit flow still behave normally.</done>
</task>

<task type="auto">
  <name>Task 2: Guard `AppContent` against `profile === null` before shell selection</name>
  <files>client/src/App.tsx</files>
  <action>Implement FE-07 per LD-08 by adding an explicit `!profile` guard after the `!user` and onboarding checks but before the admin/user shell split. Use a minimal blocking fallback that does not redirect and does not assume `brand` implies a valid profile row. Keep the current routing structure and page components intact; do not widen this into a broader auth UX redesign or global error-boundary change.</action>
  <verify>
    <automated>npm run check</automated>
    <manual>Exercise an auth/bootstrap state where `brand` exists but `profile` is null or delayed, then confirm the app shows a safe blocking fallback rather than reading `profile.is_admin`, redirecting to the wrong shell, or crashing.</manual>
  </verify>
  <done>`AppContent` no longer reaches any `profile.is_admin` decision or private shell rendering path while `profile` is still null.</done>
</task>

</tasks>

<verification>
Run `npm run check` after each task. Then manually verify direct admin entry on `/admin/dashboard` and `/admin/users`, plus a null-profile bootstrap scenario, confirming `client/src/App.tsx` now keeps admins in the admin shell and blocks unsafe rendering when `profile` is absent.
</verification>

<success_criteria>
- FE-01 is satisfied by route-syncing admin mode in `client/src/App.tsx` without removing the existing feature flag.
- FE-07 is satisfied by an explicit `!profile` guard before any admin/user shell split.
- No router redesign or `admin-mode` provider rewrite is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-reliability/04-frontend-reliability-01-SUMMARY.md`
</output>
